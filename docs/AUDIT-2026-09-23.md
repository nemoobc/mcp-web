# AUDIT 2026-09-23 — 28 temuan ronde-1 (semua FIXED)

> Metode: audit kode manual ronde-1 (P0–P4), verifikasi file:baris pada 2026-09-23 dengan **membaca kode + `git diff HEAD`** — bukan ingatan. Setiap baris "Fix" menunjuk kode yang ada di kerjaan sekarang; bila fix berupa penghapusan, ditunjuk sumber historisnya (`HEAD:<file>:<baris>`).
> Gerbang penutup: `npm test` agregat 57/57 exit 0; `npm audit` 0 vuln di kedua folder (lihat "Ronde-2" di bawah).
> **Di-update 2026-09-24: 203 test (190 server + 13 plugin) setelah build 54 tool + fix ronde critic (P1 R3: stop/timeout membatalkan I/O fase settle — `tests/stop-settle-hang.test.js`; ronde R4: timeout aset 10s screenshot — `tests/asset-timeout.test.js` + P2-1/2/3 abort mislabel & cause — `tests/abort-mislabel.test.js`; ronde R5: 8 temuan VERDICT (page-per-session, required args, css paralel, stop-shot, find ReDoS, gerbang timing, PNG ring, cap piksel) + test regresi `tests/required-args.test.js`/`tests/find-redos.test.js`/`tests/shot-tmp.test.js`; ronde R6 lama: skrip `install.sh`/`uninstall.sh` + test regresi `tests/installer.test.js`; VERDICT critic 2026-09-24 (8 temuan: OPENCODE_CONFIG bocor, bleed engine js → lock global + `tests/js-multisession.test.js`, entri uninstall tertinggal, timpa struktur senyap, hapus web milik orang, backup tertimpa, jalur fallback, exit 5) → `tests/installer.test.js` kini **19 test**; `npm test` kini `--test-concurrency=4` anti-tekanan memori)** — angka 57/57 di atas adalah gerbang ronde-1 (laporan historis).

**Rekap: P0:1 · P1:3 · P2:12 · P3:9 · P4:3 = 28 temuan — status SEMUA FIXED.**

---

## P0 (1)

| # | Temuan | Status | Fix (file:baris) |
|---|---|---|---|
| 1 | HTTP tanpa auth + bind `0.0.0.0` + CORS `*` | FIXED | Token wajib **semua** endpoint: `src/http.js:108-113` (`isAuthorized` :56-62, perbandingan timing-safe :49-54, token acak 24 byte :65). Bind default `127.0.0.1`: `src/http.js:64` + `bin/mcp-web.js:31` (`--host` tak diisi → default loopback). CORS tanpa `*`: `src/http.js:17-34` (hanya origin localhost/whitelist `MCWEB_ALLOWED_ORIGINS`), `src/http.js:36-47` (echo-only, `Vary: Origin`), preflight :101-106 tanpa ACAO `*`. Test: `tests/e2e-http.test.js:162-187` (SSE/POST/health tanpa token → 401) |

## P1 (3)

| # | Temuan | Status | Fix (file:baris) |
|---|---|---|---|
| 2 | SSRF tanpa filter + header bebas | FIXED | Guard tunggal `assertSafeTarget`: `src/security.js:51-65` (blokir non-http :58-60, host private :61-63; blokir loopback/RFC1918/link-local/metadata/IPv6-ULA/IP-desimal :12-45; bypass eksplisit `MCWEB_ALLOW_PRIVATE=1` :8-10). Dipakai di navigate `src/tools.js:68`, klik link :143, submit GET :225, navigasi awal `src/browser.js:73` + **tiap hop redirect** :89, engine-js `src/engine-js.js:233` + :249. Header bebas → `BLOCKED_REQUEST_HEADERS` (authorization/cookie/host di-strip): `src/tools.js:10-20`, dipakai :69. Test: `tests/security.test.js:10-52` (8 test SSRF) |
| 3 | Session ID `Math.random` + hijack via `MCP-Session-Id` | FIXED | ID kuat `crypto.randomUUID()`: `src/http.js:143`. Anti-hijack: ID tak dikenal → 400, tak pernah dibuat/dipakai ulang :120-127; stream masih aktif → 409 (tak ditimpa) :128-133; stream lama mati → destroy dulu sebelum ganti :134-141. Test: `tests/e2e-http.test.js:228-232` (400), :234-249 (409 anti-timpa lalu reconnect sah 200) |
| 4 | Test merah 34/37 e2e-http (health timeout) | FIXED | `waitHealth` poll retry 100 ms, budget 30 s, kirim auth: `tests/e2e-http.test.js:116-139`, dipakai :149, :158. Akar startup lambat (jsdom termuat statis) → dynamic import: `src/tools.js:48-51`. Bukti: `npm test` → 51/51 server hijau (exit 0) |

## P2 (12)

| # | Temuan | Status | Fix (file:baris) |
|---|---|---|---|
| 5 | Stack trace bocor ke klien | FIXED | `src/protocol.js:98-103` (stack → stderr :101, klien hanya pesan singkat :102) + :126 (internal non-McpError → `console.error`); `src/http.js:209-214` (stack → log server, klien `split("\n")[0]`); `src/stdio.js:27-33` (balasan error tanpa stack) |
| 6 | State setengah-jadi saat navigate non-HTML | FIXED | `src/browser.js:101-106` (content-type non-HTML ditolak **sebelum** commit state) + :107-117 (parse di variabel lokal; gagal → `clearPageState`); `clearPageState` :61-69; commit state baru :118-124. Engine-js: `src/engine-js.js:289-294` (`document.write` gagal → throw sebelum commit :308-314) |
| 7 | jsdom static import → startup ±10 dtk | FIXED | `src/tools.js:48-51` — `createTools` async, jsdom di-`import()` dinamis **hanya** saat `engine="js"`; default DOM tak pernah memuat jsdom |
| 8 | Redirect loop tanpa batas | FIXED | `MAX_REDIRECTS=10`: `src/browser.js:17` + guard :74; `src/engine-js.js:41` + guard :234 |
| 9 | Body POST tanpa limit | FIXED | `MAX_BODY` 1 MB: `src/http.js:10`; cek Content-Length **dan** akumulasi byte (chunked) → 413 + `req.resume()`: `src/http.js:169-187`. Test: `tests/e2e-http.test.js:189-193` |
| 10 | Session tak pernah dibersihkan | FIXED | TTL 30 mnt + interval sapu 60 s: `src/http.js:11-12`; sweeper buang session idle + `unref()` (tak menahan proses): `src/http.js:249-262` |
| 11 | Cookies bocor semua domain | FIXED | Listing difilter per-host halaman current (+ induk domainnya): `src/cookies.js:117-137` (`listFor`), delegasi `src/browser.js:49-52` (`getCookiesForCurrent`), tool cookies memakainya: `src/tools.js:317`. Kirim juga difilter per-URL (domain/path/secure/expired): `src/cookies.js:44-54`, :96-115 |
| 12 | `_patchGlobals` tak di-unpatch + reset rebind fetch ter-patch | FIXED | Snapshot descriptor global sekali sebelum patch pertama: `src/engine-js.js:23-39` (`GLOBAL_SNAPSHOT`/`captureGlobal`/`restoreGlobals`), fetch asli disnapshot :19-21 (`ORIGINAL_FETCH`). `reset()` → `restoreGlobals(globalThis)` :74 + `this._fetch = ORIGINAL_FETCH` :88. Setiap patch men-snapshot dulu: :203-230 |
| 13 | Queue rejected permanen (satu error matikan sesi) | FIXED | `session.queue = job.catch(...)`: `src/http.js:220-232` — request gagal dapat 400/500, rantai queue tetap resolved. stdio: `src/stdio.js:36-40` (`.catch` → stderr, queue tetap lanjut) |
| 14 | `npm start` tunjuk file mati | FIXED | `package.json:10` → `"start": "node bin/mcp-web.js serve"` (dulu `bin/webdrive-mcp.js` yang tak ada) |
| 15 | `.gitignore` tak cover `probe-*.mjs` | FIXED | `.gitignore:5` `probe-*.mjs` (+ `.env*` :6) |
| 16 | README badge basi (1.1.0 / 30-test) + `directories.doc` → `docs/` kosong | FIXED | Badge final: `README.md:5` = versi 1.2.0 + **57 test** (agregat final saat ronde-1; di-update 2026-09-24 → **203 test**), `README.en.md:5` sinkron. `directories.doc` dihapus (hanya `test` tersisa): `package.json:5-7`. `docs/` kini berisi dokumen ini |

## P3 (9)

| # | Temuan | Status | Fix (file:baris) |
|---|---|---|---|
| 17 | 3 sumber versi | FIXED | Satu sumber = `package.json`: `src/protocol.js:5-10` (baca `package.json` → `SERVER_INFO`), dipakai `src/stdio.js:3`, :8-9, `src/http.js:66`, dan User-Agent `src/browser.js:13` + `src/engine-js.js:15`, :232 (literal versi di UA sudah dilepas) |
| 18 | stdio: error `id: null` + queue tanpa catch | FIXED | Balasan error memakai ID request asli (fallback 0 hanya bila line tak terparse): `src/stdio.js:27-33`. Queue punya `.catch` pemulih → stderr, antrian tetap jalan: `src/stdio.js:36-40` |
| 19 | Atribut cookie diabaikan | FIXED | `parseSetCookie` hormati `Path`/`Domain`/`Secure`/`Expires`/`Max-Age`: `src/cookies.js:5-41` (:26-39; Max-Age menang :39). Domain attr tak cocok host respons → dibuang (anti cookie-injection lintas domain) :83; expired dibuang saat simpan :84 & saat kirim :106 |
| 20 | `bin` tak executable | FIXED | `bin/mcp-web.js` mode `100755` (`git ls-files -s`) + shebang `#!/usr/bin/env node` :1 |
| 21 | License ISC vs MIT | FIXED | `package.json:14` = `"MIT"`; `LICENSE` = MIT © 2026 Nemo; plugin ikut MIT: `plugin/browser-mcp/package.json:10` |
| 22 | click/submit tanpa validasi skema | FIXED | Validasi runtime selector: `parseSelector` `src/tools.js:34-37` dipakai click :136, fill :174, submit :204; elemen/form tak ada → error jelas :138, :176, :206. Schema `required`: click `src/tools.js:132`, fill :170, js_eval :260 |
| 23 | `vm` bukan sandbox tak didokumen | FIXED | `README.md:174` + `README.en.md:174` (Catatan keamanan); deskripsi tool `src/tools.js:254`; komentar :271-272; timeout 2 dtk :280 |
| 24 | Redirect double-log | FIXED | Satu `networkLogs.push` per hop (bukan network-log + recordResult dobel): `src/browser.js:87-91` |
| 25 | `consoleLogs`/`networkLogs` unbounded | FIXED | `CappedArray` dengan `LOG_CAP=500` (buang entri paling lama): `src/logs.js:4-14`; dipakai `src/browser.js:32-33` dan `src/engine-js.js:82-83` |

## P4 (3)

| # | Temuan | Status | Fix (file:baris) |
|---|---|---|---|
| 26 | Metode mati `get(url)` | FIXED | Dihapus dari `src/browser.js` (ada di `HEAD:src/browser.js:174` `get(url) {...}`, tak ada di kerjaan). Sisa pemanggilan-nol `get()` di engine-js juga dihapus (grep `page.get`/`get()` di `src/` + `tests/` = nol pemanggil) |
| 27 | `querySelectorAll` dobel + limit tanpa validasi | FIXED | Query **sekali** (total & potongan dari hasil sama) + limit divalidasi `Number.isInteger(limit) && limit > 0`: `src/tools.js:108-110` |
| 28 | HUKUM 12 "konten = data" tak didokumen | FIXED | `README.md:171` + `README.en.md:171` (Catatan keamanan: konten halaman = DATA, bukan instruksi / anti prompt-injection) |

---

## Ronde-2 (critique lanjutan — juga beres, di luar hitungan 28)

| Item | Status | Fix (file:baris) / bukti |
|---|---|---|
| VETO 1 — race: `wrapExecute` panggil handler tanpa antrian → `Promise.all(navigate, get_content)` baca setengah-jadi halaman senyap | FIXED | Antrian serial per-instance (satu page singleton → satu queue): `plugin/browser-mcp/index.js:20-24` (deklarasi queue), :49-67 (`job = queue.then(run)` :62 + catch pemulih `queue = job.catch(() => {})` :65 — pola `src/http.js:222-234`). Test anti-race: `plugin/browser-mcp/tests/plugin.test.js:113-126` |
| Temuan 4 — `engine=js` tak terbukti di runtime Bun | FIXED | `ALLOWED_ENGINES=["dom"]`: `plugin/browser-mcp/index.js:14`; tolak dengan pesan `engine "<x>" belum didukung plugin browser-mcp (hanya "dom")`: :36-43. Bukti: one-shot node → pesan persis, exit 0 |
| npm audit plugin = 11 moderate (`@opentelemetry/core <2.8.0` + 10 cascading, "no fix available" via semver — `@opencode/plugin@2.0.15` = versi tertinggi) | FIXED | `overrides` paksa `@opentelemetry/core@2.11.0` (versi patch rantai yang sudah ada, bukan dependensi baru): `plugin/browser-mcp/package.json:14-16`. Bukti: `npm audit` kedua folder → 0 vuln |
| License plugin ISC vs repo MIT | FIXED | `plugin/browser-mcp/package.json:10` → `"MIT"` |
| Test agregat dua folder | FIXED | `package.json:9` → `node --test --test-concurrency=4 tests/*.test.js && node --test plugin/browser-mcp/tests/*.test.js` (57 test saat ronde-1; di-update 2026-09-24 → **203 test: 190 server + 13 plugin**, exit 0) |
| README lengkap (struktur, blok plugin, baris audit, badge final) | FIXED | `README.md` + `README.en.md` sinkron; badge = angka agregat final (57 → di-update 2026-09-24 → **203**, sinkron dengan keluaran `npm test`) |
| Riset OpenCode v2 plugin tak tercatat | FIXED | `~/.config/opencode/docs/OPENCODE-V2.md` §9 (blok riset 2026-09-23) |

### SELAIN (yang TIDAK dibuktikan dokumen ini)

- Ronde-1 = audit kode statis + test offline — **bukan** pen-test eksternal, fuzzing, atau audit dependensi SDK `@opencode/plugin` selain `npm audit`.
- `vm` tetap bukan sandbox; engine `js` (jsdom) mengeksekusi script situs — risiko melekat yang didokumenkan, bukan dihilangkan.
- Keamanan runtime plugin bergantung pada mekanisme OpenCode v2 (sumber eksternal, diakses 2026-09-23).
